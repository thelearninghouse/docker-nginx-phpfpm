schemaVersion: "2.0.0"

metadataTest:
  cmd: ["/usr/bin/supervisord", "-c", "/etc/supervisord.conf"]
  exposedPorts: ["80"]
  env:
    - key: PHP_VERSION
      value: 7
    - key: PHP_CONFIG_DIR
      value: /etc/php7
  workdir: "/app"